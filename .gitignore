/samples/
/data/
/build/
/.vscode/
*.kate-swp
*.exe
/src/shared/revision.h

# Local backup copies made before editing (never meant to be committed)
*.bak
*.bak.*
*.bak-*

# Isolated worktrees for agent-driven implementation work
/.worktrees/

# Subagent-driven-development scratch: ledgers, briefs, review packages
/.superpowers/

# backlog-drain stop sentinel: user-created, deleted once honored, never meant to be committed
/docs/backlog/.stop

# Personal Claude Code profile for the autonomous /loop (bypasses permission prompts)
/.claude/settings.autonomous.json

# Local Docker environment — contains the database password.
.env
