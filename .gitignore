/samples/
/data/
/build/
/.vscode/
*.kate-swp
*.exe
/src/shared/revision.h

# Local backup copies made before editing (never meant to be committed)
*.bak
*.bak.*
*.bak-*

# Isolated worktrees for agent-driven implementation work
/.worktrees/

# Subagent-driven-development scratch: ledgers, briefs, review packages
/.superpowers/

# backlog-drain stop sentinel: user-created, deleted once honored, never meant to be committed
/docs/backlog/.stop

# Personal Claude Code profile for the autonomous /loop (bypasses permission prompts)
/.claude/settings.autonomous.json

# Local Docker environment — contains the database password.
.env

# Raw dumps from the private ops repo (ChrisMiho/turtle-tournament). Deleted
# 2026-08-12 once verified recoverable from it — these rules stay as a standing
# guard, because re-dumping to extract something is the obvious next move and
# this fork is PUBLIC. What was in them: a real DB_PASS in a backup .env, a
# third party's CGNAT home-server address, and the client-switcher workstream
# the owner ruled off this fork regardless of content.
#
# If you re-dump, keep them ignored and copy vetted files out one at a time.
/OldScripts/
/oldTests/
/OldHandoffs/
